#include <stdio.h>

main(){
	putchar(65); // numeric value of A
	    putchar(10); // newline character (\n)
	        putchar(66); // numeric value of B
	            putchar(10); // newline character (\n)
	        putchar(67), // numeric value of C
	    putchar(10); // newline character (\n)
	return 0;
}
