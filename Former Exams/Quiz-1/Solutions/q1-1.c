#include <stdio.h>

int main(void)
{

    int num;
    printf("Enter an integer.\n");
    scanf("%d",&num);

    if ( num == 0)
    {
        printf("name");
    }
    else if (num == 1)
    {
        printf("surname");
    }

    return 0;
}